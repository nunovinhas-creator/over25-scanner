# Over 2.5 Scanner — Python dependencies
# Install with: pip install -r requirements.txt
#
# Optional extras (uncomment to enable):
#   cleanlab    — label quality checks in test_picks_quality.py
#   evidently   — data/model drift monitoring (not yet wired into tests)

# Core data stack
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# Machine learning
scikit-learn>=1.3.0

# Data validation / schema
pandera>=0.17.0

# Dashboard generation
plotly>=5.17.0

# Testing
pytest>=7.4.0

# HTTP / external APIs
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# Optional: label quality detection (skip if not needed)
# cleanlab>=2.5.0

# Optional: data / model drift monitoring
# evidently>=0.4.0
